spring:
  application:
    name: java-backend
  datasource:
    url: jdbc:mysql://localhost:3306/employee_db
    username: root
    password: 1234567
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: ord.hibernate.dialect.MysqlDriver